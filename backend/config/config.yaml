server:
  port: 8888
  mode: debug # release

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "password"
  dbname: "sk-im-bot"
  sslmode: "disable"

jwt:
  secret: "your-super-secret-key-change-it"
  expire_duration: 24h

qq:
  enabled: true
  ws_url: "ws://localhost:8080" # go-cqhttp websocket headers
  access_token: ""

discord:
  enabled: true
  token: "your-discord-bot-token"
  guild_id: "" # Optional specific guild

llm:
  provider: "openai" # openai or claude
  api_key: "sk-..."
  base_url: "https://api.openai.com/v1" # Optional proxy
  model: "gpt-3.5-turbo"
  max_tokens: 1000

log:
  level: "info"
  filename: "app.log"
